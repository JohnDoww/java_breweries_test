package utils;

import lombok.Getter;

/**
 * Place for storing project requirements to do not change it accidentally and keep it up to date in one place
 */
@Getter
public class SoftwareRequirements {
    private final int AMOUNT_OF_AUTOCOMPLETE_OBJECTS_IN_RESPONSE = 15;
}
